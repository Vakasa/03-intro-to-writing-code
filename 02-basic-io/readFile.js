//----------------------------------------------------------------------------------------------------------------------
// Challenge 2: Basic Input / Output (reading files)
//----------------------------------------------------------------------------------------------------------------------

const fs = require('fs');
const path = require('path');

//----------------------------------------------------------------------------------------------------------------------

function readFile(location)
{
    const absLoc = path.resolve(location);
    const fileCon = fs.readFileSync(absLoc, { encoding: 'utf8' });
    console.log (fileCon);
}

//----------------------------------------------------------------------------------------------------------------------

readFile('./test.txt');

//----------------------------------------------------------------------------------------------------------------------
